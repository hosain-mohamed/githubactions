name : Testing Workflows

on:
  push:
    branches:
      - main

jobs:
  test-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a message
        run: echo "Welcome Hosain to Github Actions"

      - name: Checkout Repository
        uses: actions/checkout@v6.1.0

      - name: List files
        run: |
          echo "Here is My all Files"
          ls -la
